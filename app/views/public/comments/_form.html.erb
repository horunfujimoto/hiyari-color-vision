<%= form_with model: [ post, comment ], local: false do |f| %>
  <%= f.text_area :comment, rows:'10', placeholder: "コメントをここに入力してください", class:"w-100 comment-textarea" %>
  <%= f.submit "送信", class: "btn btn-dark" %>
<% end %>