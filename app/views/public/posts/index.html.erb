<div class="container py-5">
  <div class="row">
    <div class="col-11 mx-auto">
      <h3>社内ヒヤリハット一覧</h3>

      <!--並び替えのリンク「.to_unsafe_h」は検索のパラメーターをハッシュに変換して保持している-->
      <%= link_to '新着順/', posts_path(sort_by: "latest") %>
      <%= link_to '古い順/', posts_path(sort_by: "old") %>
      <%= link_to '重度順(高)/', posts_path(sort_by: "level_high") %>
      <%= link_to '重度順(低)', posts_path(sort_by: "level_low") %>

      <%= render "public/posts/list", posts: @open_posts %>

    </div>
  </div>
  <%= paginate @open_posts %>
</div>
